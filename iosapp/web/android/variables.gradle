ext {
    minSdkVersion = 23
    compileSdkVersion = 36
    targetSdkVersion = 36
    // AndroidX library versions - Updated to latest versions for Android 15 compatibility
    // Note: androidx.core:core 1.15.0 already includes edge-to-edge support APIs
    androidxActivityVersion = '1.9.3' // Latest stable - EdgeToEdgeApi28 still uses deprecated APIs internally (library issue)
    androidxAppCompatVersion = '1.7.0' // Latest stable version
    androidxCoordinatorLayoutVersion = '1.2.0' // Latest stable version
    androidxCoreVersion = '1.15.0' // Includes WindowInsetsCompat and edge-to-edge APIs
    androidxFragmentVersion = '1.8.4' // Latest stable version
    coreSplashScreenVersion = '1.0.1' // Latest stable version
    androidxWebkitVersion = '1.12.1' // Latest stable version
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.2.1'
    androidxEspressoCoreVersion = '3.6.1'
    cordovaAndroidVersion = '10.1.1'
}